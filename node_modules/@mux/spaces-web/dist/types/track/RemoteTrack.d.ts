import { Track } from './Track';
export declare class RemoteTrack extends Track {
    subscribed: boolean;
    /**
     * Subscribe to this remote track.
     *
     * If subscriptions are not being automatically managed
     * you must subscribe to a remote track before recieving media
     *
     * UNIMPLEMENTED
     */
    subscribe(): void;
    /**
     * Unsubscribe from this remote track
     *
     * If subscriptions are not being automatically managed
     * you may unsubscribe from a remote track to stop recieving media
     *
     * UNIMPLEMENTED
     */
    unSubscribe(): void;
    isSubscribed(): boolean;
}
